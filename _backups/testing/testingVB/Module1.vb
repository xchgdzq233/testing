Module Module1

    Sub Main()
        Dim FFXLocateFormID
        Dim FFXSetPageType
        Dim FFXSetFormInfo
        Dim CurrentObj
        Dim oDoc
        Dim oPage
        Dim oField
        Dim InputFormID
        Dim ValidateOrSetValue
        '''''''''''''''''''''''''''''''''''''''''''''''''''''
        '''''''''''''''''''START FROM HERE'''''''''''''''''''
        '''''''''''''''''''''''''''''''''''''''''''''''''''''





        '''''''''''''''''''''''''''''''''''''''''''''''''''''
        ''''''''''''''''''''''END  HERE''''''''''''''''''''''
        '''''''''''''''''''''''''''''''''''''''''''''''''''''
    End Sub

    Sub WriteLog(ByVal a As String)

    End Sub

    Function FindKeyList(ByVal a As Boolean, ByVal b As Boolean, ByVal c As String) As Boolean
        Return True
    End Function

    Function FFXSetFormInfo(ByVal a As String, ByVal b As String) As Boolean
        Return True
    End Function

    Function PatternMatch_Fingerprint(ByVal a As Boolean, ByVal b As Boolean, ByVal c As String) As Boolean
        Return True
    End Function

    Function UpdateField(ByVal a As Boolean, ByVal b As Boolean) As Boolean
        Return True
    End Function

    Function MetaWord(ByVal a As String) As Boolean
        Return True
    End Function

End Module
